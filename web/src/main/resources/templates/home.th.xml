<thlogic xmlns:th="urn:oasis:names:tc:entity:xmlns:xml:catalog">
    <attr sel="#customer_ref"
          th:href="@{__${T(gr.nikolis.mappings.customer.CustomerMappings).CUSTOMER_LIST}__}"/>

    <attr sel="#game_ref"
          th:href="@{__${T(gr.nikolis.mappings.game.GameMappings).GAME_HOME}__}"/>

    <attr sel="#user_ref"
          th:href="@{__${T(gr.nikolis.mappings.login.LoginMappings).USER}__}"/>
</thlogic>